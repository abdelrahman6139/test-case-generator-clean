import unittest; import logging; from selenium import webdriver; #Configure logging; logger = logging.getLogger(__name__); logger.setLevel(logging.INFO); handler = logging.FileHandler('test_log.txt'); formatter = logging.Formatter('%(asctime)s - %(levelname)s - %(message)s'); handler.setFormatter(formatter); logger.addHandler(handler); class TestLogging(unittest.TestCase): def test_log(self): driver = webdriver.Chrome(); logger.info('Test started'); driver.quit(); logger.info('Test ended'); if __name__ == '__main__': unittest.main()